apiVersion: v1
kind: DeploymentConfig
metadata:
  annotations:
    kompose.cmd: /usr/local/bin/kompose -v convert -f container-compose.yaml --provider=openshift
      --volumes hostPath --out=openshift
    kompose.version: 1.21.0 (992df58d8)
  creationTimestamp: null
  labels:
    io.kompose.service: pgadmin
  name: pgadmin
spec:
  replicas: 1
  selector:
    io.kompose.service: pgadmin
  strategy:
    resources: {}
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.network/flight-recorder-network: "true"
        io.kompose.service: pgadmin
    spec:
      containers:
      - env:
        - name: PGADMIN_DEFAULT_EMAIL
          value: pgadmin4@pgadmin.org
        - name: PGADMIN_DEFAULT_PASSWORD
          value: admin
        image: ' '
        name: pgadmin-container
        ports:
        - containerPort: 80
        resources: {}
        volumeMounts:
        - mountPath: /pgadmin4/servers.json
          name: pgadmin-hostpath0
      restartPolicy: Always
      volumes:
      - hostPath:
          path: /home/andrew/workspace/jfr-custom-events/servers.json
        name: pgadmin-hostpath0
  test: false
  triggers:
  - type: ConfigChange
  - imageChangeParams:
      automatic: true
      containerNames:
      - pgadmin_container
      from:
        kind: ImageStreamTag
        name: pgadmin:latest
    type: ImageChange
status: {}
